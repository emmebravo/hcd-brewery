{
  "name": "packages",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {},
  "scripts": {
    "client": "yarn --cwd packages/client start",
    "build": "yarn --cwd packages/client build",
    "server": "yarn --cwd packages/server server",
    "start": "yarn --cwd packages/server start",
    "dev": "run-p client server"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
